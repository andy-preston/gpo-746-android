#!/bin/bash

# adb install
# adb shell

case $1 in
'install')
    COMMAND="install -r -d share/build/build-android/outputs/apk/release/android-release-unsigned.apk"
    ;;
*)
    COMMAND="$@"
    ;;
esac

./share/platform-tools/adb $COMMAND
